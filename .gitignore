# Secrets
.env

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
.venv/
venv/

# Node
node_modules/
.next/
out/

# IDE
.idea/
.vscode/
*.swp

# OS
.DS_Store
Thumbs.db

# Local databases (SQLite)
data/*.db
data/*.db-shm
data/*.db-wal

# Output / uploads (generated runtime files)
output/*
!output/.gitkeep
mock_excels/
web/data/
uploads/
feeds/
backend/
frontend/

# Generated Google/Meta feed artifacts (never commit)
phase0/feeds/
phase0/output/
**/*.feed.xml

# Sample / ad-hoc local test scripts (keep phase0/tests/ unit tests)
/test_*.py
phase0/test_*.py
phase0/_test_samples.csv

# Build artifacts
phase0/web/*.tar.gz

# GPC SQLite is built from taxonomy-with-ids.en-US.txt on first run
phase0/data/gpc_taxonomy.db
phase0/data/gpc_taxonomy.db-*

# App Store listing — screencast is generated locally
docs/app-store-listing/screencast/

# Local dev OAuth helpers (may contain shop-specific credentials — never commit)
phase0/exchange_legacy_token.py
phase0/get_shopify_token.py
phase0/oauth_callback_server.py
phase0/oauth_full_flow.py
phase0/shopify_feed.py

# Sample data (binary Excel)
*_realistic.xlsx

# Runtime / generated (keep source only)
phase0/data/store.db
phase0/data/stores.db
phase0/data/processed_images/
phase0/scripts/.tunnel-urls

# Cursor local (optional — rules are tracked under .cursor/rules if present)
.cursor/projects/
