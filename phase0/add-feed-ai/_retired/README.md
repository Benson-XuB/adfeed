# Retired: Preact App Home UI extension (`admin.app.home.render`)

Moved out of `extensions/` so it is not deployed. Public App Store entry is the
React Router iframe app in `../web/`.

Do not re-enable `admin.app.home.render` without an explicit product decision.
Source kept for UX reference while porting remaining panels.
