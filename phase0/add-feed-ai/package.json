{
  "name": "add-feed-ai",
  "version": "1.0.0",
  "license": "UNLICENSED",
  "workspaces": [
    "extensions/*",
    "web"
  ],
  "scripts": {
    "shopify": "shopify",
    "build": "npm run build --workspace=adfeed-web && shopify app build",
    "dev": "shopify app dev",
    "info": "shopify app info",
    "generate": "shopify app generate",
    "deploy": "shopify app deploy",
    "setup": "npm run setup --workspace=adfeed-web",
    "typecheck": "npm run typecheck --workspace=adfeed-web"
  },
  "dependencies": {},
  "trustedDependencies": [
    "@shopify/plugin-cloudflare"
  ],
  "author": "xubaotian",
  "private": true
}
