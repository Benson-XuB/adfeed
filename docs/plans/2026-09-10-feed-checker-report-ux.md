# Feed Checker report UX (MVP polish)

Date: 2026-09-10  
Approved via chat: overview first, soft quality wording, CTA after fixes, shorten won’t-do.

## Ship

1. API `summary`: assessment, issue types, affected/ok product counts  
2. Soften title advice (quality / supplier-import, not “Google reject”)  
3. Frontend: overview → Issues to fix → checks cleared → AdFeed CTA  
4. Bucket UI: “N products affected”, not ambiguous leading “1”

## Out of scope

Fake green checks for fields we don’t scan (price/URL/availability).
